services:
  dockerite:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
