version: '3.8'

services:
  dockerite:
    image: ahamed1846/dockerite:latest
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
